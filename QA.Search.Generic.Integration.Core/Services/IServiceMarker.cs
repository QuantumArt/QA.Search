namespace QA.Search.Generic.Integration.Core.Services
{
    /// <summary>
    /// Базовый интерфейс для интерфейсов-маркеров, объединяющих классы одного процесса индексации:
    /// ScheduledService, IndexingContext, IndexingController, Settings, DocumentProcessor, etc.
    /// </summary>
    public interface IServiceMarker
    {
    }
}